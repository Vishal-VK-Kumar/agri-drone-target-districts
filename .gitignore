# Source data is exported by hand from UPAg and is not redistributed here.
# MANIFEST.json is the exception: it is checksums/sizes/row counts, not data.
data/raw/*
!data/raw/MANIFEST.json
data/*.duckdb
data/*.duckdb.wal

# Local secrets and environment
.env
.venv/
__pycache__/
*.pyc

# Results are regenerated, not committed. Three exceptions: output/marts/ is
# the file interface to the Power BI report, split_artefact_demo.txt is
# evidence referenced directly from the README, and finding.md is the
# write-up itself, not disposable scratch output
output/*
!output/marts/
!output/split_artefact_demo.txt
!output/finding.md

.DS_Store
